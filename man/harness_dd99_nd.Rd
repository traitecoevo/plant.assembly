% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harness.R
\name{harness_dd99_nd}
\alias{harness_dd99_nd}
\title{DD99 multi-trait harness}
\usage{
harness_dd99_nd(
  r = 1,
  K0 = 500,
  x0 = c(0, 0),
  sigma_K = c(1, 1),
  sigma_C = c(0.4, 0.4),
  trait_names = c("x1", "x2")
)
}
\arguments{
\item{r}{intrinsic growth rate}

\item{K0}{maximum carrying capacity}

\item{x0}{length-k vector of per-dimension optima (the singular strategy)}

\item{sigma_K}{length-k vector of resource-kernel widths}

\item{sigma_C}{length-k vector of competition-kernel widths}

\item{trait_names}{length-k character vector naming the traits}
}
\value{
a `harness` object
}
\description{
DD99 in two (or more) trait dimensions (cf. Ito & Dieckmann 2007).
}
\details{
The multi-trait extension of \code{\link{harness_dd99}}: the resource and
competition kernels are products of per-dimension Gaussians, so each trait
dimension `d` has its own optimum \code{x0[d]} and widths
\code{sigma_K[d]}, \code{sigma_C[d]}. The singular strategy is \code{x* = x0}
and dimension `d` is a branching direction iff
\code{sigma_C[d] < sigma_K[d]}. Use \code{trait_scale = "linear"} in
\code{community_start()} since the optima sit at 0.
}
\author{
Daniel Falster
}
